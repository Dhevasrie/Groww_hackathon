spring.application.name=hackathon

# --- H2 in-memory DB (swap to Postgres/MySQL if you need real persistence) ---
spring.datasource.url=jdbc:h2:mem:hackathondb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# H2 web console at /h2-console for quick DB inspection during dev
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

server.port=8080
